# V1.1 Revision 2 — Independent Rule Re-Gate

**审计对象**：`Geometry Battle V1.1 Playtest Rules — Revision 2`（Locked Attack Right）
**审计性质**：独立规则重审门禁 · **READ-ONLY**
**日期**：2026-09-10
**任务书**：`Plans/Input/V1.1 Revision 2 — Independent Rule Re-Gate.md`

> 本轮为全新独立审计。全程未修改任何代码、测试、规则、文档或 Playtest 产物。
> 所有结论以实际执行证据为准；仅凭代码阅读得出的结论均显式标注。
> 审计方法：主审计员内联侦察 + 16 个维度并行取证 + 每条 P0/P1/P2 由独立反驳者按不同透镜尝试推翻 + 主审计员本人另写探针独立复核。

---

# 1. Baseline

```text
Branch:                       feature/v1.1-ui-protocol
HEAD:                         0a420a8dbc7078f2d6ad584f83b247cb58b6aea9  (2026-09-10 16:35:20 +0800)
                              docs(playtest): note the frozen round state machine was left untouched
Working tree:                 已跟踪文件零修改；唯一未被 .gitignore 忽略的未跟踪文件 = 本审计任务书
                              （Plans/Input/V1.1 Revision 2 — Independent Rule Re-Gate.md）
                              gitignored 残留：artifacts/（652 场产物）、algorithms/.slots/、
                              algorithms/.staging/team-b-6410-2/、playtest/slots/
v1.0.0-competition tag:       annotated tag，对象 cdb1e8f3286dddc2558b3dd4ebb2b04dc02b3b0d
                              → commit 26d7970fbcba7b50f04e0743130ca4ffdd3bd904
                              ✓ 与任务书声称的 26d7970 一致，且从未被移到别的 commit
Revision-2 rule commit:       90647f3  docs(rules): amend shooter elimination semantics   (2026-09-10 14:54:41)
production implementation:    2e825a1  feat(v1.1): preserve locked attack after shooter elimination (2026-09-10 14:54:47)
post-fix / cleanup:           95f2a5f  refactor(v1.1): drop stale cancellation comments and runner-cancel wording
playtest result commits:      d5f7cf9 (rebalance rerun) → 124b3ad (180 matches) → e2f7c94 (report folded)
                              → 0a420a8 (frozen round state machine note)
RULE_BASELINE_SHA (pre-rev):  877f142
```

**核实结论**

1. `v1.0.0-competition` **未被移动**。tag 目标 commit 是 26d7970。唯一异常：tag 的**注解对象**曾以同名同目标被重写一次（旧对象作为 dangling `b908fc7a` 留存，taggerdate 早 8 秒）。**目标 commit 未变**，故冻结基线未受影响。
2. `90647f3` 对规则规范的改动范围经 hunk 行号映射验证：全部 16 个 hunk 落在旧文件行 972–1026，而旧 §38 = 972–995、§39 = 996–1029、§40 起于 1030 → **只改了 §38/§39，未越界**。**但这也意味着规范其余章节未被同步**（见 §2.1 / §9.2）。
3. `2e825a1` 之后只有 `95f2a5f` 碰过 `src/`（+13/−2）。其中 Match.ts 一处确为注释，但 `SandboxRunner.ts` 改的是**运行时可见的 error 字符串**（`'本轮攻击被取消'` → `'运行器被宿主取消（进程终止）—— 不是 Shooter 阵亡导致'`，`errorCode` 仍为 `CANCELLED`）。故「实现提交后 src 只被注释改动」**不成立**，应如实记为「1 处错误文案 + 1 处注释，无判定逻辑变更」。

---

# 2. Rule → Implementation Consistency

逐条对照规范 §38 / §38.1–§38.6 / §39 与真实 production implementation。**每条均有实际执行证据**（端到端 MatchEngine + sandbox-exec + 真实 Python 算法包；主审计员另跑独立场景复核）。

| # | 检查点 | 结论 | 关键证据 |
|---|---|---|---|
| a | START locks both attack rights | **PASS** | `resolveOrderedShots` 无任何 shooter-alive 守卫（`src/core/Match.ts:1398-1429`）；`order` 只由 `finalA/finalB` 决定 |
| b | first solver still attacks first | **PASS** | 主审计员独立场景：`firstSolver=A` 恒成立（tA≈19ms vs tB≈323ms），`attacksExecuted=[A,B]` 每轮 |
| c | Shooter death does not revoke attack right | **PASS** | 每轮 `shooterAliveAtAttack.B=false` 且 `attacksExecuted` 仍含 `B` |
| d | slower solver is not terminated because Shooter died | **PASS** | 后手连续存活 24/24 次采样、跑满 1232.62ms；主审计员场景 tB≈320ms（完整预算） |
| e | original Shooter remains function anchor | **PASS** | `buildCore` 用 START 快照的 shooter 位置校验 `f(x_s)=y_s`；B 收到的输入里 shooter 仍是 B1 |
| f | no same-round Shooter replacement | **PASS** | `result.shooterB === 'B1'` 即使 B1 已阵亡；无任何替换分支 |
| g | no same-round recomputation | **PASS** | 每轮 `JudgeStartRound`/`RoundComputeStart`/`RoundComputeEnd` 各恰好 1 次（2 轮 → 各 2 次） |
| h | immutable Round Snapshot preserved | **PASS** | `pendingPublic.json` 为冻结字符串，直接送入两个沙箱；结算不回写 |
| i | dead Shooter unavailable next round | **PASS** | 第 2 轮 `public_state.json` 中 A1/B1 `alive=false`；`selectShooter` 拒绝死点 |
| j | mutual elimination → DRAW | **PASS** | 确定性非 tie 场景：6/6 轮先手全为 A，第 6 轮 `aliveAfter={A:0,B:0}` → `winner=draw`、`endReason=MUTUAL_ELIMINATION` |

**增量结论（主审计员独立复核）**：在 `seed=1, pointCount=6, difficulty=easy` 的脚本化对局中，双方 6 轮各损失 1 点，**非 tie 先手顺序 6/6**，最终：

```text
round 6: firstSolver=A attacksExecuted=[A,B] shooterAliveAtAttack=A:true/B:false
         killed=[B6,A6] aliveAfter=A0/B0 mutual=true winner=draw
engine.getWinner() = draw        engine.endReason() = MUTUAL_ELIMINATION
MatchLog.winner = draw           MatchLog.endReason = MUTUAL_ELIMINATION
AuditLog: ... MutualElimination {"round":6,"aliveAfter":{"A":0,"B":0}} → MatchEnded {"winner":"draw","endReason":"MUTUAL_ELIMINATION"}
Replay: 6 帧，末帧 mutualElimination=true；persistArtifacts → loadReplay 往返无损
```

**裁决：实现侧 (a)–(j) 全部成立。**

## 2.1 但**规则文档自身**与 Revision 2 不一致（P1）

见 Findings `RC-1` / `RC-2`：规范 §61 一句话摘要仍以现在时陈述已废止的取消规则，§43 Match Win 仍无 mutual-elimination 例外，另有 §42/§51/§54/§55/§60/§62 六处清单残留。

---

# 3. Search for Legacy Cancellation

全仓搜索 `cancel` / `cancelled` / `SHOT_CANCELLED` / `shooter eliminated` / `onCancelled` / `onFirstResult` / `runner.cancel`（`grep -rni`，覆盖 `src/ tests/ competitor-kit/ algorithms/ starter/ README.md Plans/ playtest/`）。

| 命中类别 | 位置 | 分类 |
|---|---|---|
| `rounds[].cancelledA/B`、`result: CANCELLED_A/B`、`errorCode: 'CANCELLED'` | `src/core/Logs.ts:46,63,159`、`src/core/Match.ts:132,877,1355` | **HISTORICAL / COMPATIBILITY**（规范 §39 明文降级） |
| 旧规则说明文字 | `tests/locked-attack-right.ts:7`、`src/core/Logs.ts:57-65,158`、`src/core/Match.ts:872-876` | **DOCUMENTATION**（均标注为已废止） |
| UI 的「历史字段」提示 | `src/ui/AudienceScreenUI.ts:166-179` | **DOCUMENTATION** |
| `runner.cancel()` 定义与调用 | `src/runner/SandboxRunner.ts:1057,1130-1131`；`src/submission/LocalPreflight.ts:160` | **ACTIVE PRODUCTION** —— 但**仅**由 READY 握手失败触发（见下） |
| `onFirstResult` | 0 处（已删除） | **DEAD / REMOVED** |
| `SHOT_CANCELLED` / `shooter eliminated` | 0 处 | — |

**核心问题的证明**

1. `git diff 877f142 HEAD -- src/runner/SandboxRunner.ts` 显示 `DuelOptions.onFirstResult` 与 `runDuel` 的 `settleFirst()` 钩子被**完整删除**，剩下的 `Promise.all([runnerA.done, runnerB.done])` 是唯一汇合点。**没有任何一方结束会杀掉另一方。**
2. `cancelled` 字段仍影响攻击是否执行（`Match.ts:794-795` → `finalA/finalB` → `ast[team] === null` → 不执行）。但 `CANCELLED` 错误码在 `src/` 中只有**一个**产生点（`SandboxRunner.ts:901`），其唯一触发者是 `cancel()`，而 `cancel()` 在比赛路径上只有 **READY 握手失败** 一个调用点（`SandboxRunner.ts:1126-1133`）。
3. 因此「Shooter killed → opponent current shot cancelled」**不可被普通生产路径静默触发**。

**裁决：§3 PASS**（旧语义在结构上已不复存在）。

**但附带发现一个真实的活跃缺陷**（见 `PLAT-4`）：READY 握手失败会 `cancel()` **双方**，而先失败的一方已经以 `CRASH` 收尾，于是 `deriveRoundResult` 把 `CANCELLED_*` 归因给**行为正确的那一方**，与代码注释与规范 §38.5 的「唯一原因是 TIMEOUT / INVALID / CRASH」相矛盾。

---

# 4. Locked Attack Right Regression（R1–R8）+ Mutation / Negative-Control

**实跑**：`npx ts-node tests/locked-attack-right.ts` → **10/10 passed**（R1–R8 + 两条保留覆盖）。

## 4.1 Mutation 结果（主审计员本人执行的 3 组 + 维度审计员的 11 组）

| Mutation | 注入内容 | 被抓住？ | 抓住它的用例 |
|---|---|---|---|
| **M1** | `resolveOrderedShots` 循环内加回 `if (!shooter.alive) continue;` | **是** | R1, R2, R5, R6, R8（10 条中 5 条失败） |
| **M2** | `computeRound` 里「先手击杀对方 Shooter ⇒ 对方 `final` 置 null」 | **是** | R2, R6, R8 |
| **M3** | `getWinner()` 同归于尽时返回 `firstSolver` | **否（逃逸 1/5）** | 见 `VER-2` |
| M5（维度审计员） | 把 `else if (tA < tB)` 分支退化为「恒 A 先手」 | **否（逃逸 10/10）** | 见 `VER-1` |

结论：**核心取消语义的回归是有效的**——任何形式的「Shooter 死 → 攻击取消」都会被抓住。但**两条关键性质没有有效回归**：

- `VER-1`：先手顺序判定（M5「A 恒先手」逃逸全部用例）。
- `VER-2`：同归于尽不得判先手胜（M3 在末轮为 tie 时逃逸；tie 实测占约 1/3 轮次）。

R6 的探针实测（副本中打印每轮 `firstSolver`）：`["tie","A","A","B","B","B"]`、`["B","A","tie","A","A","tie"]`、`["A","B","B","tie","A","B"]` —— 末轮是否 tie 随运行波动，直接决定 M3 是否可见。

---

# 5. Runner Lifecycle

规则相关部分**全部通过**：

| 命题 | 结论 | 证据 |
|---|---|---|
| Shooter 死亡不终止慢方进程 | **PASS** | A 在 21ms 产出并击杀 B1 后，B 连续存活 24/24 采样、跑满 1232.62ms |
| 不取消其计时器 | **PASS** | B 的 `computeTimeMs` = 自己完整耗时，未被截断 |
| 不删除其 result opportunity | **PASS** | B 的 `output/result.json` 被正常接收并结算（`COMPLETE`，双 `cancelled=false`） |
| 不缩短自身 2000ms 期限 | **PASS** | 见 §6；deadline 从各自 GO 起算，双方等长 |
| `onFirstResult` 钩子彻底删除 | **PASS** | 见 §3；`killTree` 的 6 个调用点逐一核查，无一个由 Shooter 死亡触发 |

**但发现一个真实的生命周期缺陷**（`PLAT-2`）：`finish()` 未清理 `goTimeout`，所有非成功收尾路径都会泄漏该定时器，并在 `cleanupSandbox` 之后对已死 pid/进程组补发 SIGKILL。

---

# 6. Timing Semantics

**Revision 2 没有改变任何计时语义。** 经 `git diff 877f142 HEAD -- src/core/Match.ts src/runner/SandboxRunner.ts` 逐条核对：

```text
GO semantics                未变（stdin 写 'GO' → 打点 → setImmediate 关 stdin）
own timing anchor           未变（每方自己的 releaseNs）
result timing endpoint      未变（output/result.json 的 mtime）
TIE_EPS_MS = 0.05           未变（git log -S 显示历史上只有一次引入，此后未动）
first-solver ordering       未变（order 由 tA/tB 比较决定）
```

唯一与计时相邻的改动是取消分支的删除与其注释/文案。

## 6.1 Timing-fairness 实测

| 来源 | rounds | aFasterRate | startSkew 中位/p95/max | medianTimeA / B | timeDiffMs 中位 |
|---|---|---|---|---|---|
| 主审计员全量回归（含并发负载） | 300 | 0.433 | 4.500µs / 8.667µs / 112.209µs | 16.05ms / 16.02ms | 0.005 |
| 维度审计员 5 批 | 1500 | 0.677 / 0.487 / 0.480 / 0.410 / 0.507 | — | — | — |
| **汇总** | 1500 | **768/1500 = 0.512，95% CI [0.487, 0.537]** | — | — | — |

**裁决：无稳定方向偏置。** CI 覆盖 0.5；符号检验（2 高 / 3 低）p = 1.00。唯一异常批（0.677）发生在同时有 3 个 timing-fairness 进程竞争 CPU 时，其 CI [0.624, 0.730] 与 Cochran Q = 30.3 表明是负载效应，不是规则或实现的方向性偏差。**不升级为 blocking platform finding。**

已存在的、与 Revision 2 无关的边界问题见 `PLAT-3`（结果 drain grace 使 2000ms 预算在特定写法下静默缩水为 1950ms）。

---

# 7. Resolution Semantics — Algorithm Input Snapshot vs Live Resolution State

**PASS。** 五项必测点全部以实际执行证据成立（49/49 断言），采用宿主侧（patch `fs.writeFileSync`/`rmSync` 捕获沙箱字节）与算法侧（探针把读到字节与 sha256 写进 `work/observed.json`）**双向交叉验证**：

| 检查点 | 结论 | 证据 |
|---|---|---|
| 输入快照不可变 | **PASS** | 第 1 轮 `public_state.json` 对 A/B 两份 sha256 完全相同（`47405a83…`）；B 沙箱销毁瞬间的字节 = B 算法实际读到的字节 = `RoundLog.publicStateHash`，三者相等；该文件里 B1 仍 `alive=true` |
| 无 resurrection | **PASS** | 第 2 轮 `public_state.json` 里 B1 `alive=false`，名单仍为完整 12 点 |
| 无 duplicate kill | **PASS** | `killed` 无重复 id；跨轮不重复计入 |
| 无 recomputation | **PASS** | 沙箱内自增计数文件 + pid + result.json 数量三重证明：每队每轮算法体恰好执行 1 次 |
| 无 second JSON generation | **PASS** | 整场只有 12 次输入写入（3 阶段 × 2 文件 × 2 队） |

**重要界定**：「算法输入冻结 ≠ 结算状态冻结」是**设计要求**而非缺陷——规范 §38.1/§40/§41 要求函数锚点与快照在 START 后不可变，§38.6/§7 要求结算读实时存活状态（先手击杀立即生效）。本维度判 PASS。

---

# 8. Mutual Elimination

**PASS。** 独立构造了**不复用 R6** 的场景：`pointCount=8, seed=43`，双方同一「瞄准存活敌人中 id 最小者」算法，每轮各损失 1 点，第 8 轮 0/0；主审计员另用 `seed=1, pointCount=6` 构造了**非 tie 先手**的确定性场景（见 §2）。

| 表达面 | 结论 | 证据 |
|---|---|---|
| `firstSolver` ≠ 自动胜者 | **PASS** | 末轮 `firstSolver=A`（非 tie），`winner=draw` |
| MatchLog | **PASS** | `endReason=MUTUAL_ELIMINATION`、`winner=draw`、`rounds[last].mutualElimination=true`、`finalAlive={A:0,B:0}` |
| AuditLog | **PASS** | `MutualElimination{round,aliveAfter}` + `MatchEnded{winner,endReason}` |
| Replay | **PASS** | 帧内含 `mutualElimination`/`aliveAfter`；`persistArtifacts → loadReplay` 往返无损 |
| CLI / UI state model | **PASS** | 实跑 `src/operator/cli.ts`（live 与 `--replay` 两条路径）均打印「同归于尽 / WINNER: DRAW」，**无任何一方获胜的表述，也不空白** |

边界情形裁决：

- (a) 空点集分支（`endReason()` 的 `points.length===0`）是**不可达死代码**（公开 API 无法产生）。
- (b) 单方清零 → 立即以 `ELIMINATION` 结束，不会误判。
- (c) 「一方已归零但比赛继续」**不可达**（`getWinner()` 返回非 null 即结束）。

---

# 9. Competitor Kit 同步

**Revision 2 的四条核心语义已正确进入 kit**（逐条给出文件 + 行号）：

| 语义 | 结论 | 位置 |
|---|---|---|
| (a) Shooter death does not cancel locked attack | **PASS** | `competitor-kit/ALGORITHM_REQUIREMENTS.md:241`；`README.md:264` |
| (b) no replacement Shooter in same Round | **PASS** | `ALGORITHM_REQUIREMENTS.md` §6.1 |
| (c) original Shooter remains function anchor | **PASS** | `ALGORITHM_REQUIREMENTS.md` §6.1「数学发射锚点」 |
| (d) next Round dead Shooter cannot be selected | **PASS** | `ALGORITHM_REQUIREMENTS.md` §6.1 |

kit 内**不存在**把旧取消规则写成现行规则的文本；唯一一处旧规则提及（`ALGORITHM_REQUIREMENTS.md:255`）已显式标注为「规则修订说明：早期 V1.1 试玩规则曾规定…」——属合规的历史标注，不算问题。

**但 kit 的 Revision 2 同步不完整**：

- `CK-1`（P1）：§38.6 MUTUAL_ELIMINATION → DRAW **完全未同步**。`ALGORITHM_REQUIREMENTS.md` §15 把正式终止规则枚举为**只有** `ELIMINATION`；`grep -rn "MUTUAL\|DRAW\|同归于尽\|平局" competitor-kit/` 返回 **0 命中**。
- `CK-2`（P2，经反驳后由 P1 下调）：arena 边界「首次离开 → 永久终止／重新进入不恢复」的**语义**未写入 kit 的正式规则表（边界数值本身已公开）。

---

# 10. Arena Boundary

production Judge 的语义**正确**（主审计员独立执行验证）：

```text
函数 f(x) = -0.5*(x+10)*(x-5)，A 队，Shooter 在 (-10,0)
  f(-10)=0（过 Shooter）  峰值 28.125（越出 y=12）  首次越界 x≈-8.18  重新进入 x≈3.18  f(5)=0

traceTrajectory: endReason=OUT_OF_FIELD  轨迹末点=(-8.1800, 11.9938)  点数=365
judgeShot: hits=["BEFORE_EXIT (-9,7)"]
  越界后重新进入场地内的点 (5,0) 是否被错误命中？ → false   ← 规范 §34 永久终止成立
  越界前的点 (-9,7) 是否被正确命中？           → true
```

四面对照：

| 面 | 结论 |
|---|---|
| Rule Specification §34/§35 | **一致**（明文规定永久终止、重入不恢复、enemy after stop 不可命中） |
| production Judge | **一致**（已执行验证，两个方向、含重入场景） |
| playtest harness | **近似实现**，存在亚采样分歧（`PLAT-5`）；批量黑盒比对 0/4796 不一致，实用发生率近零 |
| Competitor Kit | **缺口**（`CK-2`） |

---

# 11. aBlocked / Internal Predicates — 裁决

**裁决：`aBlocked` 属于 (B) internal implementation diagnostic，不是 public competition rule semantic。不要求公开变量名。**

三条证据链：

1. **字段只存在于平台自有产物**：`src/core/Logs.ts:44-45`、赋值 `src/core/Match.ts:910`（`Boolean(shots.A?.blocked)`）；只出现在 `match.json` / `replay.json`。选手永远拿不到 match log。
2. **选手必须理解的那条规则已公开**：`competitor-kit/ALGORITHM_REQUIREMENTS.md:229-230`「…且位于**首次障碍物接触之前**」「函数轨迹先碰到障碍物 → 该次攻击被阻挡」。障碍物侧的规则语义**不存在缺口**（缺口在 Arena 边界侧，见 `CK-2`）。
3. **结构上不可能影响命中判定**：`Judge.ts:268` 的 `endX` 截断恒 ≥ `Judge.ts:269` 的 `blocked` 截断——5286 个 `blocked != null` 的 side 中 `dir*(blockX-endX)` 最小值 0、0 次违反；4564 轮里 0 个敌人落在 `(endX, blocked.x]` 区间。

**Round 2 报告的 106 / 298 已独立精确复现**，但报告把 298 的语义写含糊了：298 是 fast-vs-hybrid 这一对的 side 计数，不是「总数」。另发现 `aBlocked` 的**谓词**与 §35 给出的公开语义在 106 / 298 / 551 个 side 上不一致（`ABL-1`，P3）。

---

# 12. startSkew — 裁决

| 问题 | 答案 | 证据 |
|---|---|---|
| 已计算？ | **是** | `SandboxRunner.ts:1150-1153` |
| 已记录？ | **是** | `Match.ts:781-788`，事件 `RoundComputeEnd`，字段 `startSkew_ns` / `releaseA_ns` / `releaseB_ns` / `readySkewMs` |
| 进入 MatchLog？ | **否** | `RoundLog`/`MatchLog` 无该字段；实测 `grep -c startSkew match.json` = 0 |
| 进入 AuditLog？ | **是** | 每回合一条；随比赛默认落盘到 `artifacts/matches/<id>/audit.json`（direct-engine 与 operator CLI 两条路径均实测命中） |
| 进入 Replay metadata？ | **否** | 实测 `replay.json` grep = 0 |
| 仅存在于测试输出？ | **否** | 它就在生产审计日志里 |

**裁决：OPTIONAL。**

理由：规范 §23 只要求「平台同时记录 releaseA/releaseB/startSkew 用于审计」，实现**已满足**。正式证据链（AuditLog）已完整且默认落盘，足以支撑赛后复核同时启动公平性。缺口仅在于 MatchLog / Replay 两个「最可能被交给第三方的产物」不含该证据——建议（非必须）把三个 ns 值补进 `RoundLog`，或明确声明 `audit.json` 属官方证据包的一部分。

---

# 13. Algorithm Slot 失败裁决

**独立复现**

```text
$ npx ts-node tests/algorithm-slot.ts
  ✗ algorithm-slot: 仓库自带两个槽位结构完全一致（§2/§3/§41）
      断言失败: 槽位根目录应只有固定入口，实际 manifest.json,solver.py
        期望: ["solver.py"]
        实际: ["manifest.json","solver.py"]
algorithm-slot: 7/8 passed
```

**取证**

1. **规范 §3 原文**（`Plans/Input/V1.1 — Algorithm Slot, Startup & JSON IPC Protocol.md`）：「每支队伍**至少**必须包含：`solver.py`」，紧接着「**允许**参赛者添加自己的内部模块」（`optimizer.py` / `geometry.py` / `utils/` …）。是**下界**，不是穷举。
2. **`competitor-kit/ALGORITHM_REQUIREMENTS.md` §1**：「`manifest.json` **可选**。如果提供，必须包含 `name` 与 `version`，且 `entry`（若写）只能是 `solver.py`。」
3. **实测：只有 `solver.py` 的包被全链路接受**

   ```text
   inspectPackage(只有 solver.py): valid=true  errors=[]  manifest 自动默认
   readSlot(只有 solver.py)      : status=READY  entry=solver.py  files=1
   upload(只有 solver.py)        : {"ok":true,"hash":"50acf480…"}
   installAlgorithm(只有 solver.py): ok=true  stage="preflight"
   ```

   ⇒ **`manifest.json` 在 V1.1 不是必需文件**，平台不依赖它（`parseManifest` 强制 `entry = solver.py`，写别的入口会被明确拒绝，不构成绕过）。
4. **`assertEqual(a, b)`（§2 两槽位结构一致）上一行是通过的** —— §2 契约本身未被违反。
5. **引入点**：`d9f470c`（`docs(v1.1): sync fixed algorithm slots with canonical starter`）向两个槽位**新增** `manifest.json`，使槽位与 canonical starter 逐字节一致；`tests/algorithm-slot.ts` 自 `a81ea7f` 之后**从未再改**。该测试的最后一行断言比规范更严，且与规范 §3 的「至少 + 允许添加」相反。

**裁决：B —— OUTDATED TEST / DOCUMENTATION**（既非实现缺陷，也非契约歧义）。

**最小修复范围**：只改 `tests/algorithm-slot.ts:87` 一行断言。建议改为「根目录必须存在 `solver.py`（唯一入口），允许附加元数据与模块」，例如断言 `a.includes(ENTRY_FILENAME)` 加上一条「除 `solver.py` 外不得出现其他 `solver.py` 同名入口」的检查。上一行 `assertEqual(a, b)`（两槽位结构一致）与首行「`solver.py` 必须存在」已经分别覆盖了 §2 与 §3 的 MUST，改这一行**不会削弱任何现有保护**。

**不建议**为变绿而删除 `algorithms/team-{a,b}/manifest.json`：那会破坏 `d9f470c` 与 canonical starter 的同步意图，并与 5 个官方 fixture 包的形态不一致。

---

# 14. Full Regression

```text
$ npm run typecheck      → 退出码 0（无输出）
$ npm test               → 25/26 套件通过，EXITCODE=1
                            失败套件: algorithm-slot
```

| 套件 | 结果 | 耗时 | 套件 | 结果 | 耗时 |
|---|---|---|---|---|---|
| dsl-contract | ✓ | 0.6s | full-match-e2e | ✓ | 17.7s |
| convexity-aliasing | ✓ | 0.5s | runner-isolation | ✓ | 9.6s |
| official-starter | ✓ | 2.3s | cross-round-cheat | ✓ | 7.4s |
| map-fairness | ✓ | 0.5s | package-tamper | ✓ | 1.4s |
| obstacle-block | ✓ | 0.5s | timeout-boundary | ✓ | 7.7s |
| dual-shooter-selection | ✓ | 6.7s | process-tree-cleanup | ✓ | 2.7s |
| locked-attack-right | ✓ | 16.4s | hostile-input | ✓ | 10.0s |
| alive-kill | ✓ | 11.2s | timing-fairness | ✓ | 1357.4s |
| roundstate-equality | ✓ | 0.6s | replay | ✓ | 73.5s |
| input-protocol | ✓ | 0.5s | runtime-manifest | ✓ | 3.6s |
| stage-gating | ✓ | 8.7s | competitor-kit | ✓ | 30.6s |
| pre-start-execution | ✓ | 2.4s | **algorithm-slot** | **✗** | **7.0s** |
| preflight-decoy | ✓ | 2.9s | result-ipc | ✓ | 9.3s |

**未通过修改测试获得 PASS**；本轮未修改任何测试。

覆盖核对：`tests/` 下 29 个 `.ts` 文件，`run-all.ts` 的 SUITES 列 26 个必需的，余下 3 个是 `harness.ts` / `protocol-fixture.ts` / `run-all.ts` —— **无未覆盖的测试文件，也无列了但不存在的套件**。

**环境注记（非仓库缺陷）**：维度审计员在本机观察到 `result-ipc` 的「写完结果后不退出也不拖到 deadline」用例确定性失败（4267/7897ms > 3000ms 断言），根因经隔离证明是 macOS 对**新建可执行文件首次 exec** 的 2–3.5s 开销，而非 drain 逻辑（实测 drain 恰为 51ms）；主审计员的全量运行该套件 9/9 通过。属环境效应，记为 P3。

---

# 15. Rebalance Evidence Integrity

只读复核 `playtest/results/shooter-rule-revision/`（180 场 raw match.json + summary + harness + counterfactual）。**全部六项主张从原始数据独立重算，逐项一致。**

| 主张 | 声明的值 | 独立重算 | 一致 |
|---|---|---|---|
| matches | 180 | 3 pair × 30 条件 × 2 槽位摆位 = 180 | ✓ |
| Fast vs Optimizer | 2-38-20 | fast=2 / optimizer=38 / draw=20 | ✓ |
| Optimizer vs Hybrid | 3-37-20 | optimizer=37 / hybrid=3 / draw=20 | ✓ |
| Fast vs Hybrid | 13-10-37 | fast=13 / hybrid=10 / draw=37 | ✓ |
| INVALID / TIMEOUT / CRASH | 0 / 0 / 0 | 4496 team-rounds 全 `COMPLETE`，三项均 0 | ✓ |
| shotCancellationRate | 0 | `CANCELLED` 回合数 = 0 | ✓ |

（重算方法：按 run 目录名 `s<seed>-p<n>-<difficulty>__<alg>-A` 解析槽位归属，再用 `winner` 字段映射回算法，避免槽位互换带来的错配。）

**「三组结果与历史 CF-NO-CANCEL 的逐场一致性」主张成立**：`playtest/results/round-2/cf/sim-<pair>/sim-summary.json` 的逐场预测与 Revision-2 官方归档在 **winner 180/180、rounds 180/180** 上一致；证据文件确实存在。

发现的引用瑕疵（不影响结论）见 `BAL-2`（P2）与 3 条 P3。

---

# 16. Rule Balance Interpretation

**本轮不设计新规则，只判断现有证据支持什么。**

| 命题 | 裁决 | 数据 |
|---|---|---|
| old cancellation rule → speed/cancellation dominance | **SUPPORTED** | 旧规则官方数据（`round-2/raw`，独立重算）：Fast vs Optimizer = **Fast 50 / Opt 0 / Draw 10**，取消回合率 52.0%；Optimizer vs Hybrid = Hybrid 39 / Opt 4 / Draw 17（33.7%）；Fast vs Hybrid = Fast 26 / Hybrid 8 / Draw 26（30.7%） |
| Revision 2 no-cancel rule → optimization dominance | **SUPPORTED** | 新规则官方数据：Fast vs Optimizer = **Fast 2 / Opt 38 / Draw 20**；Optimizer vs Hybrid = Opt 37 / Hybrid 3 / Draw 20。两对对阵**胜负反转**，且取消率归零 |
| current balance problem remains unresolved | **SUPPORTED** | Fast vs Hybrid 13-10-37 —— 唯一同速对局几乎不动，平局占 58.3%；cap-60 探针 68.8% 仍不可化解（见 §17） |

**结构说明**：这一结论与 Round-2 的因果归因一致——冻结回合快照使先手顺序**无作用通道**，故在旧规则下速度影响胜负的唯一通道就是 Shot Cancellation；废除后该通道消失，优化能力成为主导。

---

# 17. Stalemate Evidence

**复核 cap-60 的 `22/32 = 68.8%`**

```text
playtest/results/shooter-rule-revision/cap60-probe/fast-vs-hybrid.json
  matches = 32（16 条件 × 2 摆位）
  exhausted(打满 60 轮) = 22   → 22/32 = 68.8%   ✓ 算术正确
  endReason 分布：NONE 22 / ELIMINATION 8 / MUTUAL_ELIMINATION 2
```

**裁决：`stalemate is a real structural problem` → SUPPORTED（就 fast-vs-hybrid 而言）。** 维度审计员独立重跑了其中 4/32 个槽位，`rounds` / `winner` / `finalAlive` / `exhausted` 与探针逐项一致，且重跑得到的 cap-60 第 30 轮状态与 cap-30 归档逐字节相同——证明探针真实，且残余僵局是 **period-1 不动点**（吸收态），不是缓慢收敛。

**但有两点必须更正：**

- `BAL-1`（P2）：分母 32 混入了 10 场**从未僵持**的对局。报告的推论句「把上限从 30 抬到 60 只化解了不到三分之一」**与事实相反**——实测抬上限**额外化解 0 场**；就真正触顶的 22 场而言是 **22/22 = 100%** 持续未决。
- 覆盖限制：本轮**只探测了 fast-vs-hybrid 一对**，探针文件自身的 `_comment` 也明确写「不应外推到其他两对」。

**本轮不决定 no-progress threshold / hard round limit。** 现有分布被 harness 上限右删失，无法从该分布识别阈值。

---

# 18. Process Contamination

**披露原文**（`playtest/results/round-2/ROUND_2_BALANCE_PLAYTEST_REPORT.md:173-177`）：

> **流程说明（主动披露）**：编制 competitor-kit 参考文档时，一个子 agent 读取了 `src/core/Judge.ts`。该处得到的内部细节（采样步长 `min(0.005, π/(8ω))`、`maxPoints`、`OBSTACLE_CONTACT_EPS`、`penetration` 约定）**已明确排除在 Hybrid 设计输入之外**。

**逐块影响范围（不写「有污染所以全盘无效」）：**

| 证据块 | 是否被污染 | 依据 |
|---|---|---|
| (i) Hybrid strategy evidence | **否（未发现传播）** | 4 项被点名的 Judge 内部常量在 competitor-kit 的每一个历史版本、`solver-hybrid`、harness 中均 **0 命中**；Hybrid 用自己的采样实现与公开的 1e-6 容差 |
| (ii) Rule causal evidence | **否（结构性不受影响）** | 因果归因是「**同一批冻结算法**在两套规则下的对照」，与算法作者是否知道 Judge 实现无关；且反事实模型以黑盒方式逐发命中平台结果 |
| (iii) Harness evidence | **否** | `gb_check.py` 不读 `src/`，自建独立采样，并以 3774 发黑盒比对自证；`blocked` 字段的例外已被开发方主动自曝（Round-2 报告 §18.1） |

**结论：污染未污染结论。** 但**排除声明本身不可审计**——「已排除」是断言而非证据，污染时点与范围无法从仓库复原。属**残余风险**，建议在 V1.1 完成波中安排一次 blind 复跑（用未参与过本轮的独立实现或换了 Judge 内部实现的变体）收口。定级 P3。

---

# 19. Findings

按 P0 / P1 / P2 / P3 分级。**相同根因的跨维度发现已合并**（合并来源在括号中标注）。

## P0 — blocks further completion work

**无。** 未发现任何阻止 V1.1 完成波继续推进的条目。Revision 2 的**生产实现**在全部 16 个维度上通过。

## P1 — must fix before tournament freeze

### RC-1 规范 §61「一句话规则摘要」仍把已废止的取消规则写成现行规则

- **Problem**：`Plans/Input/Geometry Battle V1.1 — Competition Rules & Playtest Specification.md:1715` 以现在时直陈「如果先手在后手攻击执行前击杀其 Shooter，后手本轮攻击取消」。该句是全文唯一的「一段话讲完整规则」出口，也是 90647f3 **未触及**的位置（diff 只落在旧行 972–1026）。同章 §62 第 1 条仍把「Shooter cancellation 是否需要修改」列为未决事项。附带的同类残留：§42 `Second Shot / Cancellation`（:1236）、§51 Judge 权限清单 `shot cancellation`（:1443）、§54 冻结规则清单 `Shot cancellation`（:1520）、§55 允许实验清单 `CF-NO-CANCEL`（:1531，§39 已要求改用 `CF-LEGACY-CANCEL`）、§60 `Shooter cancellation balance review`（:1696）。这些位置**都没有 §39 那样的「历史记录（不删除）」标注**。
- **Evidence**（executed + code-read）：
  ```text
  $ git diff -U0 90647f3^ 90647f3 -- "<spec>" | grep '^@@'
  全部 16 个 hunk 落在旧行 972–1026；旧 §38=972–995、§39=996–1029、§40 起于 1030 → 未越界
  $ git show HEAD:"<spec>" | grep -niE 'cancel|取消'
  1443:shot cancellation      1520:Shot cancellation     1531:CF-NO-CANCEL
  1696:Shooter cancellation balance review
  1715:…如果先手在后手攻击执行前击杀其 Shooter，后手本轮攻击取消。…
  1729:1. Shooter cancellation 是否需要修改；
  ```
  实测反证：生产路径 A 在 17.4ms 击杀 B1 后，B 仍在 1219.8ms 开火并命中 A1，`attacksExecuted=[A,B]`，**攻击未被取消**。
- **Impact**：§0 声明「平台开发 / Competitor Kit / Solver / Playtest Harness / UI / Audit 均不得擅自改变已标记 FROZEN 的规则」。§54 的冻结清单写着 `Shot cancellation`、§61 的摘要写着旧规则——按字面实现的外部方会实现被废止的规则；§55/§62 会把已决事项重新变成待决，使「当前正式规则是哪一版」不可判定。
- **Reproduction**：`git show HEAD:"Plans/Input/Geometry Battle V1.1 — Competition Rules & Playtest Specification.md" | sed -n '1711,1716p;1497,1522p;1526,1540p;1719,1737p'`
- **Recommended remediation**：在文档头部加一条全局 Revision-2 生效声明（§38/§39 为正式规则，其余章节省略）；逐条修正上述 7 处；无法判定是否为历史陈述的条目统一加 §39 式「历史记录（不删除）」标注。
- *（合并 BASE-1 / R2C-01 / R2C-02 / R2C-03 / R2C-04 / CK-SYNC-02；经 3 名独立反驳者尝试推翻，均未推翻，confidence=high）*

### RC-2 规范 §43 Match Win 未随 §38.6 更新，字面推导与 Mutual Elimination 判和冲突

- **Problem**：§38.6 新增「结算后 aliveA=0 且 aliveB=0 → MATCH DRAW / MUTUAL_ELIMINATION，不得因为 A 是 First Solver 就判 A 赢」；但 §43「Match Win」仍是旧文本「如果一方 alive = 0，则另一方 MATCH WINNER」，无任何例外。二者字面叠加时，同归于尽会同时推出「B 胜」与「A 胜」。
- **Evidence**：`git show HEAD:"<spec>" | sed -n '1249,1264p'` 显示 §43 原文；§43 的 hunk 不在 90647f3 的 16 个 hunk 内。生产实现 `endReason()` 返回 `MUTUAL_ELIMINATION` 且 `winner='draw'`（见 §8 实测）。
- **Impact**：§43 是 Match 胜负的唯一定义章节。若后续有人按 §43 重写胜负判定，会把同归于尽误判为先手胜。
- **Reproduction**：同上命令，与 `sed -n '1135,1160p'` 对读。
- **Recommended remediation**：在 §43 增加 mutual-elimination 分支并交叉引用 §38.6。

### CK-1 competitor-kit 未同步 §38.6（MUTUAL_ELIMINATION → DRAW）

- **Problem**：`competitor-kit/ALGORITHM_REQUIREMENTS.md` §15 把当前引擎的正式终止规则枚举为**只有** `ELIMINATION`，并把 STALEMATE 列为「尚未实现」；而 §38.6 新增的 `MUTUAL_ELIMINATION → MATCH DRAW` 是**已实现的生产规则**，kit 全文零提及。
- **Evidence**：
  ```text
  $ grep -rn "MUTUAL\|DRAW\|同归于尽\|平局" competitor-kit/ algorithms/README.md starter/   → 0 命中
  对照 src/core/Match.ts:870,984  const mutualElimination = aliveAfter.A === 0 && aliveAfter.B === 0;
                                 endReason: mutualElimination ? 'MUTUAL_ELIMINATION' : 'ELIMINATION'
  对照根 README.md:265（已同步）
  ```
- **Impact**：在「先手清零对方后，后手是否应把先手也清零」这一**真实策略决策点**上，kit 让选手以为后手清零只能拿到失败/无意义结果，而引擎判 DRAW。这是同一次规则修订中 kit 只拿到 §6.1、未拿到 §38.6 的同步遗漏。
- **Reproduction**：`grep -n "ELIMINATION" competitor-kit/ALGORITHM_REQUIREMENTS.md`
- **Recommended remediation**：在 §15 与 §6 规则表补一行 MUTUAL_ELIMINATION → DRAW（措辞对齐根 README.md:265），并在 `tests/competitor-kit.ts` 增加断言防止再次漏同步。

### PLAT-1 `tests/algorithm-slot.ts:87` 的过期断言使全量回归长期为红

- **Problem**：该断言要求仓库自带槽位根目录**只**含 `solver.py`，但规范 §3 只要求「**至少**包含 `solver.py`」并明文「允许参赛者添加自己的内部模块」；`algorithms/team-{a,b}/manifest.json` 是 git 已入库文件，由 `d9f470c` 为对齐 canonical starter 而新增，而该测试自 `a81ea7f` 起从未更新。
- **Evidence**：见 §13 全部取证（含「只有 solver.py 的包被 upload / readSlot / installAlgorithm 全链路接受」的实测）。
- **Impact**：回归入口 `tests/run-all.ts` 长期为 25/26、`EXITCODE=1`。真实影响是**噪声门槛**——一条假失败会掩盖后续真正的槽位回归，并使「回归全绿」这一发布信号失效。生产行为本身正确，无运行时风险。
- **Reproduction**：`npx ts-node tests/algorithm-slot.ts`（观察 7/8 与 `实际 manifest.json,solver.py`）
- **Recommended remediation**：改 `tests/algorithm-slot.ts:87` 一行。**裁决：B — OUTDATED TEST**（详见 §13）。不要为变绿而删除已入库的 `manifest.json`。
- *（合并 AS-1 / S14-F1）*

## P2 — should fix during V1.1 completion

### CK-2 competitor-kit 未公开 Arena 边界的「永久终止 / 重入不恢复」语义

- **Problem**：kit 的正式规则表（`ALGORITHM_REQUIREMENTS.md` §6，第 224–231 行）只写「位于**首次障碍物接触之前**」与「轨迹先碰到障碍物 → 该次攻击被阻挡」，**没有** Arena 边界那一条。规范 §34 的「第一次离开 y ∈ [−12,12] → 永久终止，重入不恢复，终止点之后的敌人不可命中」在 kit 中零披露。
- **Evidence**：逐份文件 grep：`ALGORITHM_REQUIREMENTS.md` arena-exit-termination = 0；`DSL_SPECIFICATION.md` = 0；`JSON_SCHEMA.md` = 0；`README.md` = 0；`starter/solver.py` = 0（唯一相关处是**非正式**的注释提示 `:128`「限制斜率，避免函数在射击区间内冲出场地」与 `:158`，且伴随 `±2.0` 的斜率限幅）。影响面：fast 的 **61.8%** 的回合以 Arena 边界终止（`shooter-rule-revision-summary.json` metrics）；维度审计员在 180 场 2248 轮上统计 **1092/4496 = 24.3%** 的已执行攻击 side 由 §34 决定终点。
- **Impact**：只读 kit 的第三方无法知道「越界即永久终止、重入不恢复」，会高估自己曲线的到达范围并误判可命中性。这是一条 **documentation blocker**，不是公平性缺陷（规则对双方对称、确定、且已冻结）。
- **Reproduction**：`cd competitor-kit && grep -rn -i "离开\|越出\|永久\|TERMINATES" *.md starter/ tools/`
- **Recommended remediation**：在 §6 规则表「障碍物」行之后补一行：「轨迹第一次离开 y ∈ [ymin,ymax] 时**永久终止**；即使同一函数随后重新进入场地，攻击也不恢复；终止点之后的敌方 Point 不可命中」，并镜像到 kit README 与根 README §6；在 `tests/competitor-kit.ts` 加断言防漂移。
- *（合并 S10-A1 / S11-ABL-2；**经 2 名反驳者复核后由 P1 下调为 P2**：边界数值本身已公开（`ALGORITHM_REQUIREMENTS.md:224`、`JSON_SCHEMA.md:61`、examples），starter 也以斜率限幅演示了规避手段；缺的只是「永久性/不恢复」这一层语义）*

### PLAT-2 `goTimeout` 未在 `finish()` 中清理：非成功收尾泄漏定时器并在清理后补发 SIGKILL

- **Problem**：`spawnRunner` 的 `finish()`（`SandboxRunner.ts:743-764`）只清理 `timeoutId` / `monitor` / `poller` / `drainTimer`，**唯独漏了 `goTimeout`**；`goTimeout` 只在「合法结果到达」这一条路径（`finishWithResult()`，:772-787）被 `clearTimeout`。于是所有非成功收尾（`INVALID_OUTPUT` / `CRASH` / `MEMORY_LIMIT` / `OUTPUT_TOO_LARGE` / `SPAWN_ERROR` / `CANCELLED`）都会把该定时器留在事件循环里。
- **Evidence**（executed，已 monkeypatch `process.kill` 打点）：
  ```text
  [   5ms] spawned A=20078 B=20079
  [2989ms] BOTH DONE  B.code=INVALID_OUTPUT B.ms=20.9
  [2991ms] cleaned; active resources 含 "Timeout"
  [5968ms] process.kill(pid=-20079, sig=SIGKILL) → ESRCH
  [5969ms] process.kill(pid= 20079, sig=SIGKILL) → ESRCH
  [5969ms] PROCESS_EXIT
  ```
  即：结果在 2989ms 已落定、沙箱在 2991ms 已删除，泄漏的 `goTimeout` 在 5968ms（晚 2979ms、晚于 cleanup 2977ms）仍发出两次 SIGKILL。CRASH 路径同样复现。反证：成功路径无残留 `Timeout`，进程立即退出。
- **Impact**：(1) 宿主进程每次非成功收尾被多拖最长 `timeoutMs`（默认 2000ms），拖慢回合/比赛收尾与各测试套件的进程退出；(2) 在沙箱销毁后仍对已死 pid/进程组补发 SIGKILL（`ESRCH` 被吞掉），仅在 pid/PGID 回绕时有误伤无关进程的理论风险；(3) **不影响任何比赛判定**（`finished` 守卫保证 outcome 不被改写）。
- **Reproduction**：见维度审计员脚本（A=fast、B=invalid、`timeoutMs=3000`，patch `process.kill` 打点）。
- **Recommended remediation**：把 `goTimeout` 的清理下沉到 `finish()`，与 `drainTimer` 并列：`if (goTimeout) { clearTimeout(goTimeout); goTimeout = null; }`（必要时把声明上移以规避 TDZ）。

### PLAT-3 结果 drain grace 使 2000ms 预算在特定写法下静默缩水为 1950ms

- **Problem**：`pollForResult` 侦测到结果后挂 `drainTimer = setTimeout(finishWithResult, RESULT_DRAIN_GRACE_MS /* 50 */)`（`SandboxRunner.ts:980-983`），但**没有清 `goTimeout`**；`goTimeout` 回调不检查 `pendingResult`。若结果在 deadline 前 50ms 内被侦测到，deadline 先到 → 判 `TIMEOUT`，尽管 `result.json` 完整且其 mtime 在预算内。
- **Evidence**（executed）：
  ```text
  写完即退：  sleep=1.960 → success=true 1983.32ms
              sleep=1.970 → success=true 1994.32ms
              sleep=1.975 → TIMEOUT 2000.00ms        ← 边界 ≈1995ms
  写完后 linger 0.3s：sleep=1.925 → success=true 1947.88ms
              sleep=1.935 → TIMEOUT 2000.00ms        ← 边界 ≈1950ms
  ```
  同一条算法，仅因写完 `result.json` **之后**是否继续运行，接受/拒绝边界平移正好 50ms。
- **Impact**：规范 §30 明确允许写完结果后继续做日志/清理，此类算法被静默扣掉最多 2.5% 预算。**不是 Revision 2 引入**：该代码在 `877f142` 与 v1.0.0 血统中完全相同。出厂 starter 写完即退，不受影响。
- **Reproduction**：见维度审计员 `probe-timeout-boundary.js`（`PROBE_LINGER_S=0.3`）。
- **Recommended remediation**：在 `goTimeout` 回调里先判 `if (pendingResult) { finishWithResult(); return; }`；或在侦测到结果时就 `clearTimeout(goTimeout)`。

### PLAT-4 pre-READY 失败把 `CANCELLED` 归因给无辜的一方

- **Problem**：`runDuel` 的 READY 握手 `catch` 会 `cancel()` **双方**（`SandboxRunner.ts:1126-1133`），但先失败的一方已以 `CRASH` 收尾；于是 `Match.ts:794-795` 的 `validA/validB` 只把存活方判为 `cancelled`，`deriveRoundResult`（:1355）随之产出 `CANCELLED_A` / `CANCELLED_B` —— **归因给行为正确的那一方**。
- **Evidence**（executed，向 B 注入 pre-READY 失败；注入只存在于 /tmp，未改仓库）：
  ```text
  result          = CANCELLED_A
  cancelled       = {"A":true,"B":false}
  aErrorCode      = CANCELLED   bErrorCode = CRASH
  attacksExecuted = []
  ```
  负对照：(a) 轮内 CRASH → `INVALID_B` / `cancelled={"A":false,"B":false}`，符合预期；(b) 极小 `memoryLimitMb` → preflight 双向拦下，门禁有效。
- **Impact**：一次平台级 pre-READY 瞬时失败（spawn 失败、sandbox-exec 启动失败、输入绑定不匹配、READY 前硬超时）会让该回合以一个**指向错误队伍**的结果码与 cancelled 标记落入比赛历史；同时它证伪了 `Match.ts:132`、`Logs.ts:57-65,158` 的「恒为 false / 生产路径不会再产出这两个值」，以及规范 §38.5 的「攻击不执行的唯一原因是 TIMEOUT / INVALID / CRASH」——`CANCELLED` 是**第四种非算法侧的不执行原因**。对评分/胜负无影响（`CANCELLED_*` 不被 `getWinner` 消费），选手也无法触发（入口冻结、无 PYTHONPATH、bootstrap 在任何选手代码前写 READY、preflight 是硬门禁），故实际发生概率低。
- **Reproduction**：维度审计员 `probe5.ts`（真实 `MatchEngine.runRound()` 路径）。
- **Recommended remediation**：让 READY 中止**对称且诚实**——双方都给同一种中止码（如 `TECHNICAL_INVALID` 或新增 `RUNNER_ABORT`），不再把 `CANCELLED` 归给存活方；或保留字段但调整回合结果码使 `CANCELLED_*` 不可达。同时修正 `Match.ts:132`、`Logs.ts:57-65,158` 的「恒为 false」表述，并让规范 §38.5 的「唯一原因」与之对齐。

### PLAT-5 harness 与 production Judge 在亚采样分辨率上可给出相反的命中判定

- **Problem**：production Judge 把轨迹截断在**最后一个在场内的采样点**，其有效停止 x 比真实交点最多短一个自适应步长 h；`gb_check.first_contact` 则二分到真实交点、并对整个攻击跨度做固定 2001 点扫描。二者停止位置不同，差异足以翻转命中判定。
- **Evidence**（executed）：同一个函数 `f(x)=20·exp(-2(x+8)²)`、同一个敌人（在真实停止点之前、场上、恰在曲线上）——`Judge hits=[]` vs `harness hits=['SLIVER']`（按规范 §35 此处**应当命中**，即 Judge 偏保守）。反向案例：一个宽约 0.0101 的窄越界，harness 固定步长 0.0150 完全跨过 → harness 漏判而 Judge 正确。第三种：`endX` 因 `Math.abs(x - xEnd) <= h` 的 break 而少走最后一步。
- **Impact**：理论上可在规范说「可命中」时判不中，或反过来在统计里记入一次虚假击杀。**实用发生率近零**——批量黑盒比对 4796 轮 0 不一致；且 `MapGenerator` 的点位 x 采样在 `[range0+1, range1-1]`，实际无法落在 ±20 附近的窄缝。
- **Reproduction**：维度审计员的 `probe.ts` / `probe3.ts` + `harness_probe.py`。
- **Recommended remediation**：让 Judge 在采样越界时二分到真实交点并把它作为轨迹末点（对齐 `firstContactX` 的 60 次二分）；或让 harness 采用同样的自适应步长与末点截断。

### VER-1 `locked-attack-right` 没有「先手顺序」的有效回归：M5「A 恒先手」逃逸全部用例

- **Problem**：套件无法区分「由实测 `computeTimeMs` 决定先手」与「先手恒为 A」。R2 断言 `firstSolver === 'A'`，但 SNIPER（17ms）天然快于 SLOW_SNIPER（422ms），该断言在 M5 下仍成立；R6 只在注释里写「先手顺序由实测耗时决定，两种顺序都合法」，从不断言具体 `firstSolver`。
- **Evidence**：mutation M5（把 `else if (tA < tB)` 分支退化为「恒 A 先手」）→ **10/10 全过**。对照 M5b（写死 `firstSolver='B'`）→ 8/10，R2/R8 失败。即断言能抓住「B 恒先手」，抓不住「A 恒先手」。
- **Impact**：若生产代码把先手顺序退化（比较写反后被特判、排序被写成常量），本套件与全仓库都不会报警。由于 §4 的核心结论（后手被杀后仍开火）在 A 恒先手时恰好也成立，这个洞**不会掩盖取消语义的回归**，但会掩盖先手顺序的回归。
- **Reproduction**：在 /tmp 副本中注入 M5 后运行 `npx ts-node tests/locked-attack-right.ts`。
- **Recommended remediation**：新增一条负向用例：A 用 sleep 算法、B 用立即求解算法，断言 `firstSolver === 'B'` 且 `attacksExecuted[0] === 'B'`。

### VER-2 R6 对「同归于尽不得判先手胜」的回归是 tie-flaky

- **Problem**：R6 构造的脚本化场景中双方用**同一份**算法同时释放，末轮 `firstSolver` 极易为 `'tie'`；而「同归于尽时让 First Solver 获胜」这类退化的可见性**完全取决于末轮是否 tie**（tie 时两条分支都返回 `draw`）。
- **Evidence**（主审计员本人执行）：注入 M3（`getWinner()` 在同归于尽时返回 `firstSolver`）后连续运行 5 次 —— **1 次 10/10 全过（mutation 逃逸）**，4 次被 R6 抓住。副本内打印每轮 `firstSolver` 的探针输出：`["tie","A","A","B","B","B"]`、`["B","A","tie","A","A","tie"]`、`["A","B","B","tie","A","B"]`、`["B","B","A","B","tie","B"]` —— 末轮是否 tie 随运行波动。
- **Impact**：§38.6 的「不得因为 A 是 First Solver 就自动判 A 赢」是本轮规则修订最核心的新增语义之一，其唯一回归在约 1/5 的运行里无法发现该语义被破坏。（**注意**：本审计已在 `/tmp` 副本上以**确定性非 tie** 场景独立验证生产实现在该路径上判 `draw` 正确，见 §2 / §8。缺口在**回归覆盖**，不在实现。）
- **Reproduction**：在 /tmp 副本中把 `getWinner()` 的同归于尽分支改为返回 `last.firstSolver`，重复运行 `npx ts-node tests/locked-attack-right.ts`。
- **Recommended remediation**：让双方使用**耗时显著不同**的算法（如一方 `sleep(0.3)` 后再求解），使末轮 `firstSolver` 恒为非 tie；或显式覆盖 `firstSolver` 的两种取值。

### BAL-1 cap-60 报告的推论句与事实相反

- **Problem**：`SHOOTER_RULE_REBALANCE_REPORT.md:377-380` 写「把上限从 30 抬到 60 只化解了不到三分之一」，任务书 §5 也引用了同一句。但分母 32 里混入了 10 场**从未僵持**的对局：16 个条件是三对触顶条件的**并集**，其中 3 个条件在 fast-vs-hybrid 从未打满 30 轮（4–6 轮即结束），另 4 场是未触顶摆位。
- **Evidence**（executed，从原始归档复算）：`fast-vs-hybrid` 触顶 22；逐场比对 cap-30 归档与 cap-60 探针 **mismatch = 0**，即探针里「60 轮内分出胜负」的 10 场在 cap-30 就已经在 4–6 轮结束，**抬上限额外化解 0 场**。
- **Impact**：方向上是保守偏差（真实持续率是 100% 而非 68.8%），不反转结论；但该句作为门禁证据是**事实错误**——它断言「约 1/3 的僵持是可收敛的」，会使门禁方对 stalemate 的严重度得出错误画像。
- **Reproduction**：从 `playtest/results/shooter-rule-revision/raw/*/summary.json` 重算触顶条件并集（16），再与 `cap60-probe/fast-vs-hybrid.json` 逐场比对 `exhausted`。
- **Recommended remediation**：改写 §13.1：明确 16 条件是三对并集、其中 3 个条件在 fast-vs-hybrid 从未触顶；把主指标改为「cap-30 触顶的 22 场中，22/22 在 cap-60 仍未决（100%）」；删除「只化解了不到三分之一」。

### BAL-2 §6/§8 对照表把「合并指标」与「配对专属指标」并列比较

- **Problem**：`SHOOTER_RULE_REBALANCE_REPORT.md` §6/§8 的 Round-2 列引用 `round-2-summary.json` 的**跨三对合并**指标（fast `killsPerValidShot` 0.5775 / `noHitRate` 0.6406；optimizer 0.285 / 0.9148），而 Revision-2 列引用**配对专属**指标。两列不是同口径。
- **Evidence**：`playtest/results/round-2/metrics-fast-vs-optimizer.json` 的配对专属 Round-2 值：fast 0.793 / 0.4291，optimizer 0.27 / 0.9333。
- **Impact**：按同口径读，结论方向**不变且更强**（fast 0.793 → 0.541 下降、optimizer 0.270 → 0.662 上升）。§16 的任何裁决都不改变。
- **Reproduction**：对比 `round-2-summary.json` 与 `round-2/metrics-fast-vs-optimizer.json`。
- **Recommended remediation**：两列统一为配对专属口径。

## P3 — polish / optional

| ID | 内容 | 证据 |
|---|---|---|
| P3-1 | `SeededRandom` 低位精度与 tag 注解对象重写等基线杂项；`v1.0.0-competition` 注解对象曾被重写（旧对象 dangling `b908fc7a`），**目标 commit 未变** | `git fsck --dangling` |
| P3-2 | 工作树存在未清理残留（`algorithms/.staging/team-b-6410-2/`、`artifacts/` 652 场、`playtest/slots/`）。全部被 `.gitignore` 覆盖，未污染版本库；但 `.staging` 残留说明安装过程异常中止时不会自动回收 | `git status --ignored` |
| P3-3 | `2e825a1` 之后 `src/` 的改动不是「只改注释」：`SandboxRunner` 的 `CANCELLED` **error 文案**变了（运行时可观测） | `git diff 2e825a1 95f2a5f -- src/runner/SandboxRunner.ts` |
| P3-4 | `aBlocked` 的**谓词**是「曲线在攻击 x 域内与障碍物存在交点」，与 §35 的公开语义在 106 / 298 / 551 个 side 上不一致；建议内部改名/澄清，不要求公开变量名 | 见 §11 |
| P3-5 | `aBlocked/bBlocked = false` 语义重载：无法区分「本轮攻击没执行」与「执行了且未被挡」 | `src/core/Match.ts:910` |
| P3-6 | cap-60 探针只归档了摘要 JSON，32 场的 match/replay/audit 当时写在 `/tmp` 未入库，聚合 `unresolvedAt60` 的步骤也无提交脚本（**可复现性成立**：16 条件子集可由 §13.1 谓词从 raw 精确重建，实测 16/16 与探针 label 集合相同；逐场可由 CLI 重跑确定性复现） | 反驳者复核后**由 P2 下调为 P3** |
| P3-7 | 条件子集来源描述含糊：探针 `_comment` 与 commit message 称「16 个打满 30 轮的条件」，实为其在三对上的**并集** | `cap60-probe/fast-vs-hybrid.json` |
| P3-8 | §13.1 的「32/96」分母未解释；§17 开放问题引用了不存在的 §13.2 | 报告原文 |
| P3-9 | `§9.2` 对 `cf-legacy-cancel` 与 Round-2 分歧的陈述理由与归档的地图哈希矛盾（3 项） | 维度审计员重算 |
| P3-10 | `§13.1` 的「decided within 60 rounds = 10」里含 2 场以 DRAW 结束的对局 | 维度审计员重算 |
| P3-11 | CF 模拟器的「死后射击」逐场计数在 2/180 场与原始归档不一致 | 维度审计员重算 |
| P3-12 | `§16.2`「速度没有实质回报」的笼统表述 | 报告原文 |
| P3-13 | `startSkew` 只存在于 `audit.json`，未进入 MatchLog / Replay（裁决 OPTIONAL，见 §12） | 实盘 grep = 0 |
| P3-14 | READY 失败路径用 `startSkew_ns = 0` 作哨兵，与真实的 0 偏置不可区分 | `Match.ts:1137-1140` |
| P3-15 | `harness.runAll` 无「零用例」守卫：0 个测试的套件打印 `0/0 passed` 并 exit 0（**本轮无实际发生的静默跳过**，26 个套件注册数均已逐一核对 > 0） | `npx ts-node -e "require('tests/harness').runAll('zero')"` → EXIT=0 |
| P3-16 | `result-ipc` 的「写完结果后不退出也不拖到 deadline」在部分环境确定性失败，根因是 macOS 对新建可执行文件首次 exec 的 2–3.5s 开销（主审计员全量运行该套件 9/9 通过） | 环境效应 |
| P3-17 | `timing-fairness` 的 `aFasterRate` 与 swap 中位耗时对机器负载敏感（3 个并发实例时 aFasterRate 达 0.677），断言为绝对值窗口 | 维度审计员 5 批观测 |
| P3-18 | 污染排除是**断言**而非证据，「已明确排除在 Hybrid 设计输入之外」无法从仓库审计（残余风险，建议 blind 复跑收口） | 见 §18 |

---

# 20. Verdict

```text
CONDITIONAL PASS
```

**判据**：本 verdict 只代表「Revision 2 被正确、一致地实现」这一命题的成立程度，不代表规则平衡已解决、赛事就绪或 V1.1 已完成。

**判为 CONDITIONAL 而非 PASS 的理由**：Revision 2 的**生产实现**在全部 16 个维度上通过 —— §2 的 (a)–(j) 十项检查点全部有执行证据；§3 的旧取消语义在结构上不可触发；§4 的核心回归有效（M1/M2 均被抓住）；§5/§6/§7/§8 的规则相关部分全部 PASS；§15/§16/§17 的平衡证据经原始数据独立重算逐项一致。**没有发现任何 P0，也没有发现任何能推翻 Revision 2 语义的生产缺陷。**

但它**不是 PASS**，因为有四项 P1 必须在赛事冻结前关闭：

1. **RC-1** —— 权威规则文档仍以现行口吻陈述已废止的取消规则（§61 一句话摘要 + 6 处清单），与 §38/§39 及生产实现直接冲突；
2. **RC-2** —— 规范 §43 Match Win 没有 mutual-elimination 例外，字面推导与 §38.6 冲突；
3. **CK-1** —— 同一次修订的 §38.6（MUTUAL_ELIMINATION → DRAW）**完全未同步进 competitor-kit**，选手在真实策略决策点上被误导；
4. **PLAT-1** —— 全量回归长期为红（25/26，`algorithm-slot`），发布信号失效。

**判为 CONDITIONAL 而非 FAIL 的理由**：「Revision 2 被正确实现」这一命题本身成立。上述四项全部位于**规则文档 / 参赛资料 / 测试**，没有一项是生产实现与 Revision 2 语义的不一致；§3 明确要求判 FAIL 的「旧规则仍可被静默触发」经证明**不成立**。因此把结论定为 FAIL 会夸大范围，定为 PASS 会掩盖四项必须关闭的门禁条件。

---

# 21. CONSOLIDATED REMAINING WORK

按依赖顺序排列，供 **V1.1 COMPLETION WAVE** 作为一次性开发输入使用。**本轮不修任何一项。**

## RULE / BALANCE

| # | 事项 | 依据 | 依赖 |
|---|---|---|---|
| R1 | 修正规范 §61 一句话摘要，改为 Revision-2 语义 | RC-1 | — |
| R2 | 修正规范 §42 / §51 / §54 / §55 / §60 / §62 六处残留清单（§55 的 `CF-NO-CANCEL` → `CF-LEGACY-CANCEL`；§62 第 1 条移出未决清单） | RC-1 | — |
| R3 | 在规范 §43 Match Win 增加 mutual-elimination 分支并交叉引用 §38.6 | RC-2 | — |
| R4 | 在文档头部增加全局 Revision-2 生效声明；对无法判定是否为历史陈述的条目统一加「历史记录（不删除）」标注 | RC-1 | R1,R2 |
| R5 | 按 §59 Rule Change Procedure 复核「规范更新应先于实现」这一流程，避免同类越序再次发生 | RC-1 | R1–R4 |
| R6 | 平衡性结论已确认（§16 三条全部 SUPPORTED），**本轮不新增平衡改动** | §16 | — |
| R7 | §13.1 改写：明确 16 条件是三对并集、主指标改为「22/22 在 cap-60 仍未决」、删除「只化解了不到三分之一」 | BAL-1 | — |
| R8 | §6/§8 对照表统一为配对专属口径 | BAL-2 | — |
| R9 | 补归档 cap-60 的原始产物与条件子集生成脚本；订正「16 个条件」的并集表述与「32/96」分母 | P3-6,7,8 | — |

## TERMINATION

> **说明**：STALEMATE / no-progress threshold / hard round limit 在规范 §62 中仍属未冻结事项，本轮**不裁决**。下列条目只是解除前置依赖。

| # | 事项 | 依据 | 依赖 |
|---|---|---|---|
| T1 | 安排一次**无上限（或上限 ≥ 1000）**的测量实验，替代被右删失的分布 | §17；Round-2 Handoff O-2 | — |
| T2 | 把 stalemate 探测扩展到 optimizer-vs-hybrid 与 fast-vs-optimizer（当前只有 fast-vs-hybrid） | §17 覆盖限制 | T1 |
| T3 | 基于 T1/T2 的数据决定是否采用 Stalemate 规则及其阈值 | §17 | T1,T2 |
| T4 | 若采用，须按 §59 补完整回归 | §17 | T3 |

## PLATFORM

| # | 事项 | 依据 | 依赖 |
|---|---|---|---|
| P1 | `goTimeout` 的 `clearTimeout` 下沉到 `finish()`（一行） | PLAT-2 | — |
| P2 | `goTimeout` 回调增加 `pendingResult` 检查，或侦测到结果时即清 `goTimeout` | PLAT-3 | P1 |
| P3 | READY 中止对称化：不再把 `CANCELLED` 归因给存活方；修正 `Match.ts:132` / `Logs.ts:57-65,158` 的「恒为 false」表述 | PLAT-4 | — |
| P4 | 让 Judge 在越界处二分到真实交点作为轨迹末点（或让 harness 对齐 Judge 的采样口径） | PLAT-5 | — |
| P5 | 统一 `aBlocked` 的内部谓词表述与命名，消除「攻击未执行」与「未被挡」的语义重载 | P3-4, P3-5 | — |
| P6 | 清理工作树残留（`algorithms/.staging/team-b-6410-2/` 等），并让安装异常中止时自动回收暂存目录 | P3-2 | — |

## COMPETITOR KIT

| # | 事项 | 依据 | 依赖 |
|---|---|---|---|
| K1 | 在 `ALGORITHM_REQUIREMENTS.md` §6/§15 补 MUTUAL_ELIMINATION → DRAW，并镜像到 kit README | CK-1 | R3 |
| K2 | 在 §6 规则表补「首次离开场地 → 轨迹永久终止（重入不恢复）」一行，并镜像到 kit README 与根 README §6 | CK-2 | — |
| K3 | 在 `tests/competitor-kit.ts` 增加断言，防止 K1/K2 再次漏同步 | CK-1, CK-2 | K1,K2 |
| K4 | 复核 kit 与 `src/operator/validate-submission.ts` 的校验口径一致性（入口文件名、manifest 可选性、必备字段） | §9 | — |

## UI / UX

| # | 事项 | 依据 | 依赖 |
|---|---|---|---|
| U1 | CLI/UI 已能正确表达同归于尽（实测 PASS）；复核「历史字段 cancelled」的展示是否会与新的 result 码语义混淆 | §8；PLAT-4 | P3 |
| U2 | 若 PLAT-4 改为对称中止码，同步更新 UI 的结果码展示 | PLAT-4 | P3 |

## TOURNAMENT OPERATIONS

| # | 事项 | 依据 | 依赖 |
|---|---|---|---|
| O1 | 明确 `audit.json` 是否属官方证据包的一部分；若是则在赛事手册中声明 | §12（OPTIONAL） | — |
| O2 | 明确 `ReleaseA/ReleaseB/startSkew` 的赛后复核流程与验收口径 | §12 | O1 |
| O3 | 明确 2000ms 预算的计时边界口径（是否含结果写盘后的 drain grace），并写入赛事手册 | PLAT-3 | P2 |

## FINAL VERIFICATION

| # | 事项 | 依据 | 依赖 |
|---|---|---|---|
| F1 | 修复 `tests/algorithm-slot.ts:87` 的过期断言，使全量回归恢复全绿 | PLAT-1 | — |
| F2 | 为「先手顺序」补一条负向回归（A 慢 B 快 → `firstSolver === 'B'`） | VER-1 | — |
| F3 | 让 R6 使用耗时显著不同的双方算法，使末轮 `firstSolver` 恒为非 tie | VER-2 | — |
| F4 | 在 `harness.runAll` 增加「零用例 → exit 1」与「期望用例数」守卫 | P3-15 | — |
| F5 | 安排一次 **blind 复跑**收口 §18 的污染残余风险 | §18 | — |
| F6 | 关闭 RC-1 / RC-2 / CK-1 / PLAT-1 后，再做一次**独立**规则重审 | §20 | R1–R4, K1, K2, F1 |
| F7 | 在 F6 通过前，不得将 `V1.1 Playtest Baseline` 宣称为 `v1.1.0-competition` | 规范 §60 | F1–F6 |

---

```text
V1.1 REVISION 2 INDEPENDENT RULE RE-GATE COMPLETE
VERDICT: CONDITIONAL PASS
PRODUCTION IMPLEMENTATION OF REVISION 2: CONSISTENT (16/16 dimensions)
BLOCKING FOR FREEZE: 4 x P1 — all in rule document / competitor kit / regression test
NEXT: V1.1 COMPLETION WAVE (see CONSOLIDATED REMAINING WORK)
```
