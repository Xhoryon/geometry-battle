# Geometry Battle V1.1 Revision 2 — Independent Rule Re-Gate

你是一个全新的独立审计 Agent。

本轮：

```text
READ ONLY
```

不得修改任何代码、测试、规则、文档或 Playtest 产物。

当前待审状态：

```text
Geometry Battle V1.1 Playtest Rules — Revision 2
```

核心规则修订：

> START 后双方本轮攻击权锁定。  
> Shooter 即使在自己的攻击执行前死亡，也不会取消本轮攻击。  
> 后手继续基于 START 时冻结的原 Shooter 与 Round Snapshot 完成计算和攻击。  
> 不发生同轮 Shooter replacement。  
> Round 结束后死亡 Shooter 正常保持死亡。  
> 双方同轮全部死亡时为 MUTUAL_ELIMINATION / DRAW。

---

# 1. Baseline

首先记录：

```text
Branch
HEAD
Working tree
v1.0.0-competition tag
Revision-2 rule commit
production implementation commit
playtest result commit(s)
```

确认：

```text
v1.0.0-competition → 26d7970
```

未被移动。

---

# 2. Rule → Implementation Consistency

逐条对照：

```text
Plans/Input/Geometry Battle V1.1 — Competition Rules & Playtest Specification.md
```

Revision 2 与真实 production implementation。

重点检查：

```text
START locks both attack rights
first solver still attacks first
Shooter death does not revoke attack right
slower solver is not terminated because Shooter died
original Shooter remains function anchor
no same-round Shooter replacement
no same-round recomputation
immutable Round Snapshot preserved
dead Shooter unavailable next round
mutual elimination → DRAW
```

不得只依据开发 Handoff 判断。

---

# 3. Search for Legacy Cancellation

全仓库搜索生产路径中的：

```text
cancel
cancelled
SHOT_CANCELLED
shooter eliminated
onCancelled
onFirstResult
runner.cancel
```

分类为：

```text
ACTIVE PRODUCTION SEMANTIC
HISTORICAL / COMPATIBILITY
TEST
DOCUMENTATION
DEAD CODE
```

Revision 2 不允许仍存在可被普通生产路径触发的旧：

```text
Shooter killed
→ opponent current shot cancelled
```

语义。

如果旧规则仍可被静默触发：

```text
FAIL
```

---

# 4. Locked Attack Right Regression

独立运行并检查新的：

```text
locked-attack-right
```

回归。

必须至少验证：

```text
R1
A kills B Shooter
B already solved
→ B attacks

R2
A kills B Shooter
B still computing
B later returns valid result before its deadline
→ B attacks

R3
B later TIMEOUT
→ no B attack because TIMEOUT, not cancellation

R4
B returns INVALID
→ no B attack because INVALID

R5
both Shooters may kill each other in same Round

R6
aliveA=0 and aliveB=0 after resolution
→ MUTUAL_ELIMINATION / DRAW

R7
dead Shooter unavailable next Round

R8
no same-round Shooter replacement
```

不要只运行测试。

至少对关键测试做 mutation / negative-control 验证，确认断言真的能够抓住旧 cancellation 行为。

---

# 5. Runner Lifecycle

检查 SandboxRunner / orchestration。

确认 Shooter death 不再导致：

```text
terminate slower participant process
cancel its timer
delete result opportunity
shorten its own 2000ms deadline
```

Runner 生命周期只能依据正常：

```text
valid result
timeout
invalid
crash
cleanup
```

结束。

---

# 6. Timing Semantics

确认 Revision 2 没有顺便改变：

```text
GO semantics
own timing anchor
result timing endpoint
TIE_EPS_MS
first-solver ordering
```

运行当前 timing-fairness suite。

记录：

```text
aFasterRate
swap delta
startSkew
median compute times
```

如果只是已知统计刀锋波动，应如实说明。

如果发现稳定方向偏置：

升级为 blocking platform finding。

---

# 7. Resolution Semantics

重点确认：

```text
Algorithm Input Snapshot
```

与：

```text
Live Resolution State
```

没有被错误混淆。

例如：

```text
START:
A1, A2, B1, B2 alive

A attacks first:
kills B1

B attacks second:
its algorithm/function remains based on START snapshot
```

但正式存活结果中：

```text
B1 remains dead
```

不得：

- resurrection；
- duplicate kill；
- recomputation；
- second JSON generation。

---

# 8. Mutual Elimination

独立构造至少一个双方在同一 Round 全灭的场景。

确认：

```text
first solver ≠ automatic winner
```

最终必须：

```text
DRAW
endReason = MUTUAL_ELIMINATION
```

并检查：

```text
MatchLog
AuditLog
Replay
CLI/UI state model
```

是否能够表达这一状态。

---

# 9. Competitor Kit

确认 Revision 2 已同步到公开参赛资料。

参赛者必须能够明确知道：

```text
Shooter death does not cancel current locked attack
no replacement Shooter in same Round
original Shooter remains function anchor
next Round dead Shooter cannot be selected
```

不能继续出现会误导参赛者的：

```text
Shooter assassination cancels opponent shot
```

当前规则描述。

---

# 10. Arena Boundary

确认已经公开的：

```text
first arena boundary exit
→ permanent trajectory termination
```

在：

```text
Rule Specification
Competitor Kit
production Judge
playtest harness
```

之间一致。

如果存在真实 Judge 行为但 Competitor Kit 未公开：

记录 documentation blocker。

---

# 11. aBlocked / Internal Predicates

检查 Round-2 报告提到的：

```text
aBlocked
```

究竟属于：

```text
public competition rule semantic
```

还是：

```text
internal implementation diagnostic
```

如果参赛者必须理解它才能正确预测攻击：

则必须公开其规则语义。

如果只是内部字段：

不要求公开变量名。

明确给出裁决。

---

# 12. startSkew

确认：

```text
startSkew
```

是否：

- 已计算；
- 已记录；
- 是否进入 Match/Audit log；
- 是否进入 Replay metadata；
- 是否仅存在测试输出。

参赛算法不需要收到 startSkew。

但审计员需要判断：

> 正式比赛是否应该保留足够证据供赛后复核同时启动公平性。

给出：

```text
BLOCKER / IMPORTANT / OPTIONAL
```

裁决。

---

# 13. Algorithm Slot 25/26 Failure

独立裁决当前：

```text
algorithm-slot
```

失败。

已知现象：

```text
test expects slot root contains only solver.py

but current slots contain:
solver.py
manifest.json
```

不得相信开发方“测试过期”或“实现错误”的任何预判。

检查：

```text
current Algorithm Slot specification
Competitor Kit
algorithms/README.md
slot installer
manifest usage
tests
historical commits
```

最终必须明确选择：

```text
A. IMPLEMENTATION DEFECT
```

或：

```text
B. OUTDATED TEST / DOCUMENTATION
```

或：

```text
C. CONTRACT AMBIGUITY
```

并给出最小修复范围。

这是本轮必须裁决的问题。

---

# 14. Full Regression

运行：

```text
npm run typecheck
npm test
```

记录真实：

```text
passed suites
failed suites
```

不得通过修改测试获得 PASS。

---

# 15. Rebalance Evidence Integrity

只读复核：

```text
SHOOTER_RULE_REBALANCE_REPORT.md
```

及对应 raw evidence。

至少确认：

```text
180 matches
Fast vs Optimizer = 2-38-20
Optimizer vs Hybrid = 3-37-20
Fast vs Hybrid = 13-10-37
0 INVALID
0 TIMEOUT
0 CRASH
shotCancellationRate = 0
```

并确认三组结果与历史 CF-NO-CANCEL 的逐场一致性主张是否成立。

---

# 16. Rule Balance Interpretation

本轮不要设计新规则。

只判断当前证据是否支持：

```text
old cancellation rule → speed/cancellation dominance
Revision 2 no-cancel rule → optimization dominance
```

以及：

```text
current balance problem remains unresolved
```

回答：

```text
SUPPORTED / NOT SUPPORTED / INCONCLUSIVE
```

---

# 17. Stalemate Evidence

复核当前：

```text
cap-60
22/32 = 68.8% still unresolved
```

及其数据来源。

判断是否足以支持：

```text
stalemate is a real structural problem
```

但不得在本轮决定最终：

```text
no-progress threshold
hard round limit
```

---

# 18. Process Contamination

Round 2 曾披露一个子 Agent 越权读取：

```text
src/core/Judge.ts
```

评估这是否污染：

```text
Hybrid strategy evidence
Rule causal evidence
Harness evidence
```

不得简单写“有污染所以全盘无效”。

明确给出影响范围。

---

# 19. Final Finding Classes

所有发现只能归入：

```text
P0 — blocks further completion work
P1 — must fix before tournament freeze
P2 — should fix during V1.1 completion
P3 — polish / optional
```

避免大量细碎同级 issue。

相同根因的发现合并。

---

# 20. 最终 Verdict

选择：

```text
PASS — REVISION 2 IMPLEMENTATION CONSISTENT
```

或：

```text
CONDITIONAL PASS
```

或：

```text
FAIL
```

注意：

这里的 PASS 只代表：

> Revision 2 被正确、一致地实现。

它不代表：

```text
rule balance solved
tournament ready
V1.1 complete
```

---

# 21. Final Output

生成：

```text
Plans/Output/V1.1 Revision 2 Independent Rule Re-Gate.md
```

最后必须给出一个：

```text
CONSOLIDATED REMAINING WORK
```

章节。

只列真正尚未解决的 V1.1 工作，按依赖顺序分成：

```text
RULE/BALANCE
TERMINATION
PLATFORM
COMPETITOR KIT
UI/UX
TOURNAMENT OPERATIONS
FINAL VERIFICATION
```

不要修。

这份清单将直接成为下一阶段：

```text
V1.1 COMPLETION WAVE
```

的一次性开发输入。