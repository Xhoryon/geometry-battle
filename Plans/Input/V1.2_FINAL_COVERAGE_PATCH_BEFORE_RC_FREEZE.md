# Geometry Battle V1.2 — Final Coverage Patch Before RC Freeze

> **Status:** Final development coverage pass before independent RC audit  
> **Branch:** `feature/v1.2-interactive-tournament`  
> **Mode:** Conservative / targeted / no scope expansion  
> **Release tag:** Do **not** create one in this wave

---

## 1. Current Accepted State

The current V1.2 platform stabilization is accepted.

Known baseline:

- `typecheck`: PASS
- `typecheck:web`: PASS
- `npm test`: 35/35
- `npm run e2e`: 3 passed, run successfully twice
- Tournament Mode: PASS
- participant upload: PASS
- source review: PASS
- participant-selected Emitter: PASS
- Emitter lock/privacy: PASS
- Judge workflow: PASS
- Spectator/Replay: PASS
- stored ZIP browser E2E: PASS
- demo direct/sandbox reproducibility: PASS
- demo period-2 strategy issue: OPEN but NON-BLOCKING

This wave must **not** reopen already completed platform-correctness work without new evidence.

---

## 2. Goals of This Wave

The objectives are strictly limited to:

1. Add real DEFLATE ZIP upload coverage.
2. Perform one final development-side verification pass.
3. Establish the current branch as the Final RC Candidate baseline.
4. Update handoff/context documentation.
5. Finish with a clean working tree.
6. Do **not** create a release tag.

Do not expand the scope beyond these goals unless a real bug is exposed by the new coverage.

---

## 3. Before Changes

Run:

```bash
git status --short
git branch --show-current
git rev-parse HEAD
git log --oneline -8
```

Requirements:

- branch must be `feature/v1.2-interactive-tournament`;
- working tree must be clean.

If the working tree is not clean:

- investigate first;
- do not reset;
- do not discard;
- do not overwrite unknown changes.

Record the current HEAD as the pre-patch baseline.

---

## 4. DEFLATE ZIP Coverage

The existing real browser ZIP E2E already covers a stored ZIP.

Now add coverage for an actual DEFLATE-compressed ZIP.

The objective is **not** to test a compression implementation.

The objective is to exercise the real product path:

```text
real DEFLATE ZIP bytes
→ browser upload
→ server ZIP parser
→ decompression
→ stripCommonRoot
→ package validation
→ transactional install
→ source listing
→ source preview
→ Preflight
```

The test must genuinely enter the DEFLATE parsing/decompression branch.

Do not mock the branch.

---

## 5. Fixture Strategy

Use the simplest and most stable fixture strategy.

Preferred example:

```text
tests/fixtures/uploads/
└── valid-deflate.zip
```

The archive should be intentionally small.

Example structure:

```text
my-algorithm/
├── solver.py
└── manifest.json
```

or the smallest package that satisfies the repository's current competitor-package contract.

If the repository already has an established upload-fixture directory, follow the existing convention instead of creating a competing structure.

---

## 6. Do Not Add Heavy Dependencies

Do not add a large production dependency solely to create one test archive.

Preferred options:

1. use an existing project dependency/capability;
2. use a small test-only helper;
3. generate the fixture once and commit it;
4. use a lightweight development-time mechanism.

If a binary ZIP fixture is committed, add a small assertion proving that its compression method is actually DEFLATE.

This prevents someone from later repacking it as `stored` while leaving the filename `valid-deflate.zip`.

---

## 7. Real Browser E2E Requirement

The DEFLATE coverage must be a genuine browser upload flow.

The test should:

1. open the participant page;
2. select `valid-deflate.zip` through the file input;
3. upload using the real browser path;
4. wait for server processing;
5. confirm upload success;
6. confirm package/file listing;
7. open `solver.py`;
8. confirm source preview content;
9. run the relevant validation / Preflight;
10. confirm the submission reaches the expected ready state.

Do **not** bypass the browser and call the upload endpoint directly while calling the result E2E.

API/unit coverage may exist in addition to the browser test, but it does not replace the browser test.

---

## 8. Preserve Existing Stored ZIP Coverage

Do not replace the existing stored-ZIP test with the new DEFLATE case.

Both execution branches should remain covered:

```text
STORED ZIP
DEFLATE ZIP
```

These are distinct parser paths.

---

## 9. Invalid ZIP Transactionality

Check whether current E2E coverage already proves:

```text
existing valid submission
→ participant uploads invalid replacement ZIP
→ replacement is rejected
→ previously valid submission remains installed and authoritative
```

If already covered through the real product path, do not duplicate it unnecessarily.

If not covered, add one minimal test.

The important invariant is:

```text
reject before destructive install
```

Do not verify only that the UI displays an error.

Also verify that the server-authoritative submission identity/hash was not replaced.

---

## 10. ZIP Security Regression

Do not expand this wave into a ZIP-security redesign.

Simply confirm that existing tests continue to cover:

```text
../ traversal rejected
absolute path rejected
invalid package rejected
```

If already covered, leave the implementation alone.

If an obvious gap is discovered, add only the smallest targeted regression.

---

## 11. Preserve Protocol Anti-Drift Work

Do not modify the accepted protocol boundary:

```text
official participant obstacle JSON
= protocol form using cx / cy

compatibility reader
= can normalize both cx/cy and center:[x,y]
```

The compatibility reader must not become justification for changing official output bytes.

Do not redesign the obstacle schema in this wave.

---

## 12. Demo Solver Is Out of Scope

Do not modify demo strategy in this wave.

Specifically, do not work on:

```text
period-2 limit cycle
target scoring
waypoint scoring
strategy optimization
demo win rate
```

The existing deterministic `demo-repro` regression must continue to pass.

The demo strategy weakness is non-blocking for this release candidate.

---

## 13. UI Redesign Is Out of Scope

Do not start a new visual redesign.

Only make a minimal UI fix if the DEFLATE browser E2E exposes a genuine product bug.

Do not casually:

```text
restyle whole pages
rename architecture
move major components
rewrite upload flow
change unrelated layout
```

---

## 14. Targeted Verification First

After adding DEFLATE coverage, run the relevant targeted tests first.

At minimum, depending on what changed:

- ZIP parser/upload tests;
- participant upload tests;
- relevant browser E2E;
- `typecheck` / `typecheck:web` if TypeScript changed.

First prove that the new DEFLATE case itself passes.

Do not jump directly to the full suite before confirming the targeted path.

---

## 15. Anti-Vacuity / Mutation Check

You must prove the new test really exercises the DEFLATE branch.

Perform at least one temporary mutation.

Examples:

```text
temporarily reject DEFLATE
```

or:

```text
temporarily break DEFLATE decompression
```

The new test must become red.

Then restore the correct implementation.

Do not commit the mutation.

Record that the anti-vacuity check was performed.

---

## 16. Final Development Gate

After targeted verification succeeds, run:

```bash
npm run typecheck
npm run typecheck:web
npm test
npm run e2e
```

Record:

```text
exit code
suite count
E2E count
duration
```

Do not hide failures through shell pipelines or ignored exit codes.

---

## 17. Timing-Sensitive Failure Policy

If a timing-sensitive suite fails once:

Do not immediately classify it as flaky.

Follow the current development rules:

1. record the failure;
2. inspect global machine load;
3. determine whether multiple timing-sensitive suites slowed simultaneously;
4. restore a normal environment;
5. rerun the complete gate once.

Only after a clean full rerun in a normal environment may the original failure be classified as environmental interference.

Do not use system load as a blanket excuse for timeout failures.

---

## 18. Real Browser Tournament Rehearsal

After automated verification, perform at least one real browser tournament flow:

```text
Team A upload
Team B upload
source review
Preflight
Emitter selection
LOCK
Judge READY
match start
multiple rounds
Spectator
match end
Replay
```

At least one participant must use a real DEFLATE ZIP.

The normal tournament flow must not require terminal intervention.

---

## 19. Commit Strategy

This wave should ideally produce one focused commit:

```text
test(web): cover deflate zip tournament upload
```

If the DEFLATE E2E exposes a genuine product bug that requires a code fix, use a separate commit such as:

```text
fix(upload): <concise description>
```

Do not mix unrelated documentation formatting, broad UI changes, or cleanup into the code/test commit.

---

## 20. Update Context / Handoff

Update:

```text
docs/agent-context/PROJECT_STATE.md
docs/agent-context/V1.2_CURRENT_HANDOFF.md
```

Record the actual new baseline.

Do not mechanically preserve old counts such as:

```text
35/35
3 E2E
```

if the new test changes them.

Examples:

```text
npm test: 36/36
npm run e2e: 4 passed
```

or, if DEFLATE is added within an existing test and count remains unchanged, record the unchanged count truthfully.

---

## 21. RC Candidate Marker

Do **not** create a Git release tag.

In the handoff document, record:

```text
V1.2 FINAL DEVELOPMENT RC CANDIDATE

HEAD: <actual SHA>
BRANCH: feature/v1.2-interactive-tournament
```

This is documentation only.

It is not a release tag.

---

## 22. Push Development Branch

If all of the following are true:

- full gate passes;
- browser rehearsal passes;
- working tree is clean;

then the development branch may be pushed:

```bash
git push -u origin feature/v1.2-interactive-tournament
```

Forbidden:

```text
force push
release tag creation
moving existing tags
rewriting V1.1 history
```

If push fails because of authentication or permission:

- report the failure;
- do not use a dangerous workaround;
- do not rewrite history.

---

## 23. Final Cleanliness Check

Run:

```bash
git status --porcelain
git rev-parse HEAD
git log --oneline -8
```

Confirm:

- no temporary ZIP artifacts;
- no debug traces;
- no runtime-slot pollution;
- no modified archived playtest evidence;
- no accidental screenshot garbage;
- no release-tag movement;
- no untracked test leftovers.

The final working tree must be clean.

---

## 24. Required Completion Report

On success, report the factual final state in this structure:

```text
V1.2 FINAL DEVELOPMENT COVERAGE COMPLETE

Branch:
feature/v1.2-interactive-tournament

Final HEAD:
<sha>

DEFLATE ZIP:
PASS

Stored ZIP:
PASS

Transactional invalid replacement:
PASS / already covered

Typecheck:
PASS

Tests:
<n>/<n> PASS

Browser E2E:
<n> passed

Real tournament rehearsal:
PASS

Working tree:
CLEAN

Push:
<status>

Release tag:
NOT CREATED

READY FOR INDEPENDENT FINAL RC AUDIT
```

If the DEFLATE path exposes a real product bug:

1. fix the bug;
2. add a regression;
3. rerun the targeted and full gates;
4. only then report RC readiness.

Never bypass a failing path just to reach RC.

---

## 25. Stop This Development Agent After Completion

Once the agent reports:

```text
READY FOR INDEPENDENT FINAL RC AUDIT
```

this development agent should stop.

Do not continue feature work in the same agent.

The next step is a **fresh, independent Final RC Auditor Agent**.

That auditor should:

- not continue development by default;
- not assume the development report is correct;
- independently inspect the actual HEAD;
- independently verify the critical release surfaces;
- issue one of only three outcomes:

```text
PASS — APPROVED FOR V1.2 RELEASE

CONDITIONAL PASS — <specific, limited blocker>

FAIL — <P0/P1 blocker>
```

The independent audit should focus on:

```text
competition rules were not altered by UI work
Tournament Mode is server-enforced
upload replacement is transactional
Emitter privacy is transport-enforced
Judge / Team / Spectator projections are correctly separated
source viewer rejects traversal and leaks no host paths
refresh/reconnect behavior is safe
Replay is isolated and read-only
stored ZIP works
DEFLATE ZIP works
runtime artifacts do not pollute tracked source
documentation matches implementation
release/tag safety is preserved
```

Do not create the release tag until that independent audit passes.
